---
import SolutionSection from "./_SolutionSection.astro";
import { solutions } from "../content/solutions.ts";

const solution = solutions.find((s) => s.id === "teemo");
if (!solution) throw new Error("Missing 'teemo' entry in solutions.ts");
---

<SolutionSection solution={solution} index={2} />
