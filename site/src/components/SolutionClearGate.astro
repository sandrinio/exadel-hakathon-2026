---
import SolutionSection from "./_SolutionSection.astro";
import { solutions } from "../content/solutions.ts";

const solution = solutions.find((s) => s.id === "cleargate");
if (!solution) throw new Error("Missing 'cleargate' entry in solutions.ts");
---

<SolutionSection solution={solution} index={1} />
